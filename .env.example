# ─── labmate-mcp v4.0 — Environment Variables ───
# All variables are OPTIONAL. The server works without any keys.
# Tools requiring missing keys will show setup instructions.

# ─── Core (recommended) ───
# OpenAlex — polite pool (10 req/sec vs 1 req/sec)
OPENALEX_EMAIL=your.email@university.edu

# Semantic Scholar — dedicated rate limit
# Get free key: https://www.semanticscholar.org/product/api#api-key
S2_API_KEY=

# Unpaywall — open access PDF lookup (100k/day)
# Falls back to OPENALEX_EMAIL if not set
UNPAYWALL_EMAIL=

# ─── Optional (unlock extra tools) ───
# Materials Project — search_materials_project tool
# Get free key: https://materialsproject.org (register → dashboard → API key)
MP_API_KEY=

# IBM RXN for Chemistry — predict_retrosynthesis tool
# Get free key: https://rxn.res.ibm.com (register)
RXN_API_KEY=

# EPA CompTox Dashboard — search_toxicity tool
# Request free key: email ccte_api@epa.gov
COMPTOX_API_KEY=

# Web of Science — search_web_of_science tool
# Requires institutional subscription: https://developer.clarivate.com
WOS_API_KEY=
